# Heap / Priority Queue

A heap is a tree-shaped structure usually stored in an array.

## In this section

- `Heap Properties`: parent-child order rules
- `Push and Pop`: maintaining the heap while updating
- `Heapify`: building a heap efficiently
